@page "/category"
@using DevShop.Data
@inject IUnitOfWork uow
@inject TreeBuilder treeBuilder
@attribute [Authorize(Roles = "7")]
@layout ManagementLayout



<h1>Categories</h1>
@if (!string.IsNullOrEmpty(treeViewMarkup.Value))
{
    <div>
        @treeViewMarkup
    </div>
    <br /><br />
    <a class="mIcon mIconNew backSignalColor hoverBackDarkSignalColor" href="./category/create"></a>
}
else
{
    <span>
        Loading ...
    </span>
}